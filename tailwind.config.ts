// Tailwind v4: theme is configured in globals.css via @theme {}
// This file is kept for IDE compatibility but is not used at runtime.
import type { Config } from 'tailwindcss'
const config: Config = {}
export default config
